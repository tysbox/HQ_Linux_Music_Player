# V3.0 Phase 0-4 最終点検記録

- **点検対象**: `5704e325` までのV3.0修正
- **点検日時**: 2026-09-25
- **安全backup**: `.safety_backups/phase0-4-20260925-183130/`
- **unit stage**: `/tmp/hqm-phase-units-20260925-183158/`

## 判定

コード・静的検証・GUI build・hq-api/GUIの単独再起動は問題ありません。CamillaDSPについては、設定YAML不在という既存の起動障害が解消していないため、CamillaDSP.serviceの正常稼働は未確認です。

## 検証結果

| 項目 | 結果 |
|---|---|
| `python3 -m compileall` | 成功 |
| `tests.unit.test_hqmplayer_core` | 25 tests 成功 |
| `scripts/check_sync.sh` | 成功 |
| shell syntax check | 成功 |
| `git diff --check` | 成功 |
| `npm run lint` | 0 errors / 8 warnings |
| `npm run build` | 成功 |
| hq-api.service | active / 8002 LISTEN |
| audiophile-new-gui.service | active / 3003 LISTEN |
| mpd.service | active / 6600 LISTEN |
| camilladsp.service | activating (auto-restart) / 1234未LISTEN |

## API / GUI確認

```text
GET /health       200
GET /              200
GET /api/devices   200
GET /api/now_playing 200
GET new-gui /     200
主要JS asset      200
```

`/health` は以下の degraded 状態です。

```json
{"status":"degraded","mpd":"connected","dsp":"disconnected","loopback":true,"bt_sink":false}
```

## 残存する問題

```text
/tmp/camilladsp/active_dsp.yml が存在しない
```

CamillaDSP journal:

```text
Could not open config file '/tmp/camilladsp/active_dsp.yml'.
Reason: No such file or directory
```

このため、CamillaDSP.service は以下の状態のままです。

```text
activating (auto-restart)
```

CamillaDSPの再起動、YAML生成、ALSA切替、MPD output切替は実施していません。これらはYAML生成・音声経路の復旧方針を別途確認してから行う必要があります。

## 実施済みPhase

- Phase 0: backup・現状記録完了
- Phase 1: unit stage / verify完了（実配置なし）
- Phase 2: hq-api.service単独再起動成功
- Phase 3: audiophile-new-gui.service単独再起動成功
- Phase 4: CamillaDSPは安全条件により未実施

## Rollback

hq-apiとGUIの再起動は正常復旧済みです。CamillaDSPについてはunitを再起動していないため、CamillaDSPの既存unitを退避した状態を維持しています。YAMLを復旧した後に、CamillaDSPの単独再起動と音声確認を別途実施してください。
