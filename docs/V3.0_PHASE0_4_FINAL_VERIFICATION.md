# V3.0 Phase 0-4 最終点検記録

- **点検対象**: `5704e325` までのV3.0修正
- **点検日時**: 2026-09-25
- **安全backup**: `.safety_backups/phase0-4-20260925-183130/`
- **unit stage**: `/tmp/hqm-phase-units-20260925-183158/`

## 判定

コード・静的検証・GUI build・hq-api/GUIの単独再起動に加え、OSリブート後の自動起動も確認済みです。CamillaDSPは退避済み設定から有効なYAMLを生成し、単独復旧後に正常稼働を確認しました。

## 検証結果

| 項目 | 結果 |
|---|---|
| `python3 -m compileall` | 成功 |
| `tests.unit.test_hqmplayer_core` | 25 tests 成功 |
| `scripts/check_sync.sh` | 成功 |
| shell syntax check | 成功 |
| `git diff --check` | 成功 |
| `npm run lint` | 0 errors / 8 warnings |
| `npm run build` | 成功 |
| hq-api.service | active / 8002 LISTEN |
| audiophile-new-gui.service | active / 3003 LISTEN |
| mpd.service | active / 6600 LISTEN |
| camilladsp.service | active / 1234 LISTEN |

## API / GUI確認

```text
GET /health       200
GET /              200
GET /api/devices   200
GET /api/now_playing 200
GET new-gui /     200
主要JS asset      200
```

リブート後の確認では、全サービスが `enabled` かつ `active` であり、3003・8002・6600・1234がLISTENであることも確認しました。`/health` は以下の正常状態です。

```json
{"status":"ok","mpd":"connected","dsp":"connected","loopback":true,"bt_sink":true}
```

CamillaDSPの復旧時は、退避済みの設定から生成した `/tmp/camilladsp/active_dsp.yml` を構文検証してから配置し、`camilladsp.service` のみを単独復旧しました。

## 実施済みPhase

- Phase 0: backup・現状記録完了
- Phase 1: unit stage / verify完了
- Phase 2: hq-api.service単独再起動成功
- Phase 3: audiophile-new-gui.service単独再起動成功
- Phase 4: CamillaDSP YAML生成・構文検証・単独復旧成功
- OSリブート後の自動起動成功

## Rollback

OSリブート後も、hq-api、GUI、MPD、CamillaDSPが自動起動し、8002・3003の疎通とDSP/MPD接続を確認済みです。CamillaDSPの復旧元設定は安全バックアップとして保持されています。
