// NowPlayingBar is no longer used as a separate component in DMP.
// The now-playing section is embedded directly in page.tsx
// to share the DAP aesthetic (silver panel + oak frame).
// This file is kept as a no-op export to avoid import errors.
export function NowPlayingBar() { return null }
